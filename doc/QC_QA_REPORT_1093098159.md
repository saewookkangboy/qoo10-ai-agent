# 서비스 QC/QA 리포트 — 상품 페이지 (g/1093098159)

**대상 URL**: https://www.qoo10.jp/g/1093098159  
**상품명**: ホイップド 【低刺激スクラブ】ヴィーガンパックスクラブ 210g（3種類）  
**검토 일자**: 2026-02-20  
**활용 에이전트**: Crawl, Analysis, Recommendation, Checklist, Validation, Report-Output, Manual(선택)

---

## 1. 요약

| 구분 | 건수 | 비고 |
|------|------|------|
| **누락 데이터** | 4 | 추출 실패·미수집 항목 |
| **보완 필요 데이터** | 5 | 규칙·선택자·스키마 보강 필요 |
| **오류·위험 데이터** | 3 | 정규화 URL 차단, 검증 빈값 등 |

---

## 2. 누락 데이터 (Missing Data)

| # | 항목 | 설명 | 담당 에이전트 | 권장 조치 |
|---|------|------|----------------|------------|
| 1 | **상품 옵션 재고/가격** | Type별 재고(マグツリー 543, ムファバター 215, ラボカド 165), (1+1) 옵션 가격(+1,495円) 등이 크롤 스키마에 없음 | Crawl | `product_data.options` 또는 `variants` 스키마 정의 후 선택자/JS로 옵션·재고·추가가격 수집 |
| 2 | **商品Q&A 건수** | 페이지에 "商品 Q&A(19)" 표시, 현재 리뷰/설명 위주로 추출되어 Q&A 개수 미수집 | Crawl | `_extract_qa_count` 또는 page_structure에 qa_count 패턴 추가 |
| 3 | **配送先·発送国** | "発送国: 韓国", "お届け先の国", "配送先: 上海" 등 배송 대상 지역/발송국 정보 미추출 | Crawl | shipping_info에 `ship_from_country`, `delivery_region` 등 필드 보강 |
| 4 | **メニュー얼 커버리지** | 메뉴얼 파이프라인 미실행 시 큐텐 대학 한국어 메뉴얼 대비 누락 섹션/링크 미파악 | Manual | `test_data_pipeline_full.py --manual` 정기 실행 또는 CI에 메뉴얼 검증 단계 추가 |

---

## 3. 보완해야 하는 데이터 (Data to Improve)

| # | 항목 | 현재 상태 | 목표 | 담당 에이전트 | 권장 조치 |
|---|------|-----------|------|----------------|------------|
| 1 | **리뷰 총개수** | "4.8 (1,063)"에서 1,063 추출 — 이미지/표시 리뷰만 10건으로 잘못 나오던 이슈 수정됨. 다만 정규식 `[\d,]+` 외 "レビュー (1,063)" 패턴 이중화 권장 | review_count = 1063 안정 수집 | Crawl | `_extract_reviews`에서 "レビュー\s*[（(]\s*([\d,]+)" 우선 매칭 유지, 단위 테스트로 1,063 검증 |
| 2 | **요소 단위 검증** | `elements_validation`이 크롤러 `page_structure.elements_detail`과 분석 `page_structure_analysis.elements` 매핑에 의존. elements_detail 비어 있으면 검증 빈 배열 | 요소별 crawl_present vs analysis_present 일치율 산출 | Validation | Crawl 단계에서 elements_detail 채우기 보장(.spec-kit/product-page-elements-spec.md 기준), 없을 때 fallback 메시지 |
| 3 | **썸네일/상세 이미지** | 썸네일·상세 이미지 및 alt 수집 로직 보강됨. itemGoods·detail 선택자와 Playwright JS 병합 시 중복 제거·alt 빈값 처리 일관화 | has_thumbnail True, detail_images ≥1, alt 활용 | Crawl, Analysis | analyzer `_analyze_images`에서 image_count = detail_images + item_goods_images 중복 제거 검증 |
| 4 | **체크리스트 항목–요소 매핑** | 체크리스트 항목이 element_id와 연결되어 리포트에 반영. 일부 항목(쿠폰, 배송 등)이 페이지 구조와 불일치할 수 있음 | 항목별 element_id 및 present/score 정확도 | Checklist | 메뉴얼·스펙의 필수/선택 요소와 체크리스트 item_id 매핑표 유지, 불일치 시 suggestion 출력 |
| 5 | **POWER MINISHOP RATE** | Shop 페이지에서 "POWER 95%" 등 레이트 표시가 이미지/스타일로만 있으면 텍스트 추출 실패 → shop_level normal로 나올 수 있음 | power/excellent 정확 추출 | Crawl (Shop) | crawler_shop에서 POWER 이후 80자 이내 N% 패턴·전각 ％ 확장 적용됨; 추가로 aria-label·data-* 속성 탐색 검토 |

---

## 4. 오류·위험 데이터 (Error / Risk Data)

| # | 항목 | 내용 | 영향 | 권장 조치 |
|---|------|------|------|------------|
| 1 | **정규화 URL 연결 리셋** | `/g/1093098159`를 `gmkt.inc/Goods/Goods.aspx?goodscode=1093098159`로 변환 후 Playwright 접속 시 `net::ERR_CONNECTION_RESET` 발생 가능 | 상품 크롤 실패 | **적용함**: 정규화 URL 실패 시 **원본 URL(/g/XXXXX)로 재시도** (crawler.py) |
| 2 | **검증 시 elements_validation 빈 배열** | page_structure.elements_detail이 없으면 요소 단위 검증이 비어 있어 검증 점수만으로는 요소 누락을 구분하기 어려움 | QC 시 요소별 불일치 미노출 | Crawl에서 elements_detail 필수 수집 또는 Validation에서 빈 배열일 때 "요소 미수집" 경고 메시지 추가 |
| 3 | **API 스키마 정규화 실패 시** | product_data가 API 스키마와 맞지 않을 때 `normalize_crawler_data_to_api_structure` 실패하면 structure_comparison 없이 검증 진행 | 검증 결과는 나오나 구조 일치도 미제공 | DataValidator에서 예외 시 structure_comparison=null 유지, 로그 레벨 경고로 남기고 검증은 계속 진행(현행 유지 권장) |

---

## 5. 페이지 기준 기대값 vs 파이프라인 (참고)

| 필드 | 페이지 표시 | 기대값 (test_data_pipeline_full) | 비고 |
|------|-------------|-----------------------------------|------|
| product_code | 商品番号: 1093098159 | 1093098159 | ✓ |
| sale_price | 販売価格 2,990円 | 2990 | ✓ |
| original_price | 参考価格 3,300円 | 3300 | ✓ |
| review_count | 4.8 (1,063) | 1063 | ✓ (쉼표 처리 반영) |
| rating | 4.8 | 4.8 | ✓ |
| has_thumbnail | 상단 이미지 | True | ✓ |
| detail_images | 商品画像·상세 | ≥1 | ✓ |
| オプション・在庫 | Type별 재고, (1+1) 가격 | — | 미수집(누락) |
| 商品Q&A | 19 | — | 미수집(누락) |

---

## 6. 서비스 개선 로그 반영 항목

- **크롤러**: Playwright 시 정규화 URL 실패 시 원본 `/g/` URL 재시도 적용.
- **검증**: elements_detail 미존재 시 elements_validation 빈 배열 대신 안내 메시지 추가 권장.
- **데이터 스키마**: 옵션·재고·Q&A 건수·배송 지역 필드 스키마 확장 후 수집 로직 추가 권장.
- **메뉴얼**: `--manual` 실행으로 메뉴얼 커버리지 정기 점검 권장.

---

**문서 버전**: 1.0  
**최종 수정**: 2026-02-20
